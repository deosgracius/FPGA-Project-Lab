# FPGA Project Lab

Verilog coursework and project work from the **FPGA / Digital Systems Project Lab**
(Texas Tech University, ECE). The work builds toward an **autonomous line-following
rover** on a **Digilent Basys 3** board (Xilinx Artix-7, 100 MHz), developed in
**Xilinx Vivado**.

> This repository contains **source only** — Verilog (`.v`), constraints (`.xdc`),
> Vivado project files (`.xpr`), and a little embedded C. All Vivado build artifacts
> (`*.cache`, `*.runs`, `*.sim`, `*.hw`, `*.ip_user_files`, `.Xil`) are excluded; they
> are regenerated by Vivado on demand. See [How to use](#how-to-use).

## What's here

Two source trees, kept as they were organized during the course:

### `ProjectLab/` — main project work (rover, group projects, iterations)
The bulk of the work, evolving toward a final autonomous rover. Notable pieces:

- **Rover / navigation** — `Rover Navigation/`, `Rover_Navigation/`, `Toward Final/`,
  `Trial1 Main Project/`, `navigation2.v`, `Rover_LF_TR_Only.v`: top-level controllers
  that tie sensors → navigation state machine → motor/servo outputs.
- **Sensing** — IR and proximity/line sensors (`IR_Test/`, `Sensor_another.v`,
  `Group1/sensor.v`, sensor modules throughout).
- **Actuation** — DC motor + PWM and servo control (`Dc Motor and VG Switch Test/`,
  `DC_Prox_Servo_Test/`, `Servo/`, `motor_control.v`), with debounce / edge-detect
  helpers.
- **Group projects** — `Group1/`, `Group10/` (other lab groups' submissions), plus
  combined experiments (`group1234567.v`).
- **ALU learning project** — `ALU.v` and related (`alu_top`, `mux_8to1`, `add`,
  `shift_left/right`, `and/or/invert` ops with testbenches): a from-scratch ALU used
  to learn structural/behavioral Verilog.
- **Embedded C** — `EmbeddedSys/Code.c` (microcontroller-side companion code).

### `ProjectLab1/` — early/learning iterations
- **`LearningVivado/`** — first Vivado project: state machines, motor control, and
  sensor modules while learning the toolchain.
- **`project_1` … `project_7`** — successive iterations of the line-follower
  (sensor behavior, motor control, state-case FSMs) with simulation testbenches
  (`*.srcs/sim_1/new/*Testbench*.v`).

## Subsystems (recurring modules)

| Subsystem | Representative modules |
|---|---|
| Sensor input | `sensor`, `sensor_module`, `SensorBehavior`, `ir_receiver`, `proximity_sensor` |
| Navigation FSM | `state_m`, `state_machine`, `State_cases`, `rover_controller`, `autonomous_robot` |
| Motor control | `motor_control`, `dc_motor_controller`, `pwm_generator` |
| Servo | `servo`, `pwm_servo`, `servo_controller` |
| Display / utils | `seven_seg_decoder`, `debounce`, `edge_detector` |
| ALU (learning) | `alu_top`, `mux_8to1`, `add`, `shift_left`, `shift_right`, logic ops |

## Hardware target

- **Board:** Digilent Basys 3
- **FPGA:** Xilinx Artix-7 (xc7a35t)
- **Clock:** 100 MHz on pin W5 (see the `*.xdc` constraint files)
- **I/O:** switches, push-buttons, LEDs, seven-segment display, Pmod headers for
  sensors and the motor driver

## How to use

Each project folder is a Vivado project. To open one:

1. Launch **Vivado** (the projects were built on Vivado; open the matching version
   or let Vivado upgrade the project).
2. **File → Open Project** and pick that project's `*.xpr`, **or** create a new
   Basys 3 project and add the `.v` sources from `*.srcs/sources_1/new/` plus the
   `.xdc` from `*.srcs/constrs_1/new/`.
3. Run synthesis → implementation → generate bitstream, then program the board.

The build directories are intentionally not committed; Vivado regenerates them.

## Notes

- Folder and file names (including some `- Copy` duplicates and work-in-progress
  iterations) are preserved as-is to keep the development history of the lab.
- Group submissions (`Group1`, `Group10`) include work by other lab groups, kept for
  reference and completeness.
